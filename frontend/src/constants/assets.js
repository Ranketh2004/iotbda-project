import babyInfantPlaceholderUrl from '../assets/baby-infant-placeholder.svg?url';

/** Bundled URL (content-hashed in production) so the default baby avatar always updates after deploy. */
export const DEFAULT_BABY_PLACEHOLDER_SRC = babyInfantPlaceholderUrl;
